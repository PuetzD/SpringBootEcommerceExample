spring:
  jpa:
    show-sql: true
  flyway:
    enabled: true
    locations:
      - classpath:db/migration
      - classpath:db/demo
logging:
  level:
    org:
      springframework:
        security: TRACE
      hibernate:
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE