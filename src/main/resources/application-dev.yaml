spring:
  jpa:
    show-sql: true
  flyway:
    enabled: true
logging:
  level:
    org:
      springframework:
        security: TRACE
      hibernate:
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE